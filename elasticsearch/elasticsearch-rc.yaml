apiVersion: v1
kind: ReplicationController
metadata:
  name: elasticsearch
  labels:
    app: elasticsearch
spec:
  replicas: 1
  selector:
    name: elasticsearch
  template:
    metadata:
      labels:
        name: elasticsearch
    spec:
      containers:
      - name: elasticsearch
        image: elasticsearch
        imagePullPolicy: "Never"
        ports:
        - containerPort: 5858
      nodeSelector:
        training: "true"
